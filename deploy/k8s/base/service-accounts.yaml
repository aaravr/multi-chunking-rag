apiVersion: v1
kind: ServiceAccount
metadata:
  name: idp-agent
  namespace: idp-platform
  labels:
    app.kubernetes.io/part-of: idp-platform
  annotations:
    # For AWS IRSA or GCP Workload Identity — add IAM role ARN here
    # eks.amazonaws.com/role-arn: arn:aws:iam::123456789:role/idp-agent
    # iam.gke.io/gcp-service-account: idp-agent@project.iam.gserviceaccount.com
    {}
automountServiceAccountToken: false
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: idp-streamlit
  namespace: idp-platform
  labels:
    app.kubernetes.io/part-of: idp-platform
automountServiceAccountToken: false
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: idp-ingestion
  namespace: idp-platform
  labels:
    app.kubernetes.io/part-of: idp-platform
automountServiceAccountToken: false
